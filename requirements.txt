
# Audio      : soundfile, librosa, pydub, pylibrb
# GUI        : configparse, ipywidgets, tqdm
# Inference  : numpy, scipy, ml_collections, onnxruntime-gpu

# Needed for both PC and Colab (missing packages)

# NOTE : PyTorch_CUDA is already installed on Colab !

configparse

pydub
# Rubber Band
pylibrb

onnxruntime-gpu

# ONLY for MDX23C models
# ml_collections
# yaml

# MP3 Tags   : mutagen (for future use)
# mutagen
